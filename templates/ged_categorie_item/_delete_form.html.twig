<form method="post" action="{{ path('app_ged_categorie_item_delete', {'id': ged_categorie_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ged_categorie_item.id) }}">
    <button class="btn">Delete</button>
</form>
