{% extends 'base.html.twig' %}

{% block title %}GedCategorie{% endblock %}

{% block body %}
    <h1>GedCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ged_categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ged_categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ged_categorie_index') }}">back to list</a>

    <a href="{{ path('app_ged_categorie_edit', {'id': ged_categorie.id}) }}">edit</a>

    {{ include('ged_categorie/_delete_form.html.twig') }}
{% endblock %}
